// This file is a placeholder for AI-generated code.
// Paste the generated component code into the AI Sandbox and click "Save".

import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Beaker } from "lucide-react";

export default function GeneratedCard() {
  return (
    <Card className="h-full border-dashed border-primary">
        <CardHeader>
            <CardTitle className="flex items-center gap-2 text-primary">
                <Beaker className="h-5 w-5" />
                Sandbox Output
            </CardTitle>
        </CardHeader>
      <CardContent>
        <p>This is the initial state. Paste code into the AI Sandbox card and save it to see the result here.</p>
      </CardContent>
    </Card>
  );
}
